spec:
  template:
    spec:
      volumes:
      - name: grafana-oauth-config
        configMap:
          name: grafana-oauth-config
      containers:
      - name: grafana
        volumeMounts:
        - name: grafana-oauth-config
          mountPath: /etc/grafana/provisioning/auth
          readOnly: true
